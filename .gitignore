bin/
coverage.out

# env files
.env
.env.*
!.env.example

# agent dev artifacts
.agent/
.claude/worktrees
# Harness session directory. Holds the session transcript, which includes the full
# environment the agent was spawned with (live API keys). Never commit it.
.pi/
